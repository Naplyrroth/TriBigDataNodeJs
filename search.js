module.exports = {
    searchKeyWord: (file, keyWord, genre) => {
        console.log('Search key word function');
        console.log(`File: ${file}`);
        console.log(`Key word: ${keyWord}`);
        console.log(`Genre: ${genre}`);
    }
}
